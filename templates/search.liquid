{% comment %}
  To edit the contents of this file, look inside the `sections` folder for the file name that comes directly after `section`. Eg. {% section 'product__main' %} or {% section 'page__banner' %}
{% endcomment %}

<div id="template-{{ template | replace: '.', '-' | handle }}" class="shopify-section search-page" data-check-for-order="true">
  {% comment %} Search banner {% endcomment %}
  {% section 'search__banner' %}
  <section class="section__wrapper
                  section
                  ">
    <div class="container breadcrumb__container">
      <div class="one-whole column has-padding-bottom">
        <div class="breadcrumb__wrapper">
          {% include 'breadcrumb', type: 'search' %}
        </div>
      </div>
    </div>

    {% comment %} Search title {% endcomment %}
    <header class="container hide-when-banner-enabled">
      {% capture heading_title %}{{ 'general.search.title' | t }}{% endcapture %}
      {% include 'heading', title: heading_title, heading_tag: 'h1', context: 'search-page', text_alignment: 'left' %}
    </header>

    {% comment %} Search sidebar and main content area {% endcomment %}
    <div class="container search__content">
      {% section 'search__sidebar' %}
      {% section 'search__main' %}
    </div>
  </section>
</div>
